apiVersion: v1
kind: Service
metadata:
  name: kunal-app-service
spec:
  selector:
    app: kunal-app
  ports:
  - protocol: TCP
    port: 80  # Port exposed by the service
    targetPort: 80  # Target port inside the container
  type: LoadBalancer  # To expose the service to the outside world
